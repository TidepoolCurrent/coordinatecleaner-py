[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "coordinatecleaner-py"
version = "0.1.0"
description = "Python port of R's CoordinateCleaner for cleaning occurrence record coordinates"
readme = "README.md"
license = {text = "MIT"}
authors = [
    {name = "TidepoolCurrent", email = "the.tidepool.current@gmail.com"}
]
requires-python = ">=3.9"
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Scientific/Engineering :: Bio-Informatics",
]
dependencies = [
    "numpy>=1.20",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
]

[project.urls]
"Homepage" = "https://github.com/TidepoolCurrent/coordinatecleaner-py"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
